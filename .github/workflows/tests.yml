name: Test Laravel with Pest and React with Playwright

on:
  push:
    branches:
      - staging
      - main
  pull_request:
    branches:
      - staging
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    environment: testing
    services:
      redis:
        image: redis:latest
        ports: ['6379:6379']
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.2
          extensions: pdo, pgsql, pdo_pgsql, redis
          tools: composer:v2

      - name: Install Composer deps
        run: composer install --no-interaction --prefer-dist --optimize-autoloader

      - name: Copy .env
        run: cp .env.testing .env

      - name: Generate key
        run: php artisan key:generate --env=testing

      - name: Run migrations
        run: php artisan migrate --force

      - name: Install JS deps
        run: npm ci

      - name: Build assets
        run: npm run build

      - name: Remove Vite hot file
        run: rm -f public/hot

      - name: Setup Cloudinary configuation
        run: |
          echo "" >> .env.testing
          echo "CLOUDINARY_URL=${{secrets.CLOUDINARY_URL}}" >> .env.testing
          echo "CLOUDINARY_CLOUD_NAME=${{secrets.CLOUDINARY_CLOUD_NAME}}" >> .env.testing
          echo "CLOUDINARY_API_KEY=${{secrets.CLOUDINARY_API_KEY}}" >> .env.testing
          echo "CLOUDINARY_API_SECRET=${{secrets.CLOUDINARY_API_SECRET}}" >> .env.testing
          echo "CLOUDINARY_SECURE_URL=${{secrets.CLOUDINARY_SECURE_URL}}" >> .env.testing

      - name: Setup Laravel reverb configuration
        run: |
          echo "" >> .env
          echo "REVERB_APP_ID=${{secrets.REVERB_APP_ID}}" >> .env.testing
          echo "REVERB_APP_KEY=${{secrets.REVERB_APP_KEY}}" >> .env.testing
          echo "REVERB_APP_SECRET=${{secrets.REVERB_APP_SECRET}}" >> .env.testing
          echo "REVERB_HOST=${{secrets.REVERB_HOST}}" >> .env.testing
          echo "REVERB_PORT=${{secrets.REVERB_PORT}}" >> .env.testing
          echo "REVERB_SCHEME=${{secrets.REVERB_SCHEME}}" >> .env.testing

      # - name: Run Laravel tests
      #   run: php artisan test --coverage

      - uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests
        run: npx playwright test

      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
